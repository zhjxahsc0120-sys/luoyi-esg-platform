# 对方「精简开发版」引入建议 V0.1

**日期：** 2026-07-25  
**阅读方：** Cursor（独立核读 RAR，核对 Trae 报告）  
**RAR：** `e:\xwechat_files\wxid_ra4sdyvn1j2522_ee41\msg\file\2026-07\20260722精简开发版(1).rar`（约 75MB）  
**解压位置（已删除）：** `%TEMP%\ref_20260722_jingjian\`（分析后清理，未提交任何 vendor 源码到 `src/`）

---

## 1. 阅读确认

| 项 | 结论 |
|----|------|
| RAR 是否读到 | **是**（WinRAR `UnRAR.exe` 解压成功） |
| 项目身份 | 根目录名 `6a53a1b3d0f497e311ecc95f_精简开发版`；`package.json` name = **`luoyi-esg-dashboard`** |
| 技术栈（一句话） | **与本仓库同族**：Vue 3 + Vite 5 + TypeScript + Pinia + SCSS + ECharts + Cesium，Flask/MySQL 后端；含 `dist/`、`node_modules/` 的 2026-07-22 前后快照 |
| 与「外部竞品」关系 | **不是另一套产品**，是本项目的「精简/较早开发版」快照；本仓已在其上演进出 E01–E04 / S02 地图工作台等 |

**结构速览（对方有、且与 Trae 报告相关）：**

- `src/components/assistant/`：6 组件齐全  
- `src/composables/useMotionMode.ts`、`src/components/master/MasterKpiSubCard.vue`  
- `src/components/modal/S02–S04、G01–G04`  
- `src/modules/traffic-gis-overview/`  
- `server/mysql_build_v1.0_esg_modification/`（本仓目录缺失）  
- `src/views/DataIngestionPage.vue`、`CarbonFootprintPage.vue` + 路由 `/ingestion`、`/carbon`（本仓路由已去掉）  
- **无** `src/components/e01|e02|e03|s02` 工作台、**无** `server/e_group`

---

## 2. 对 Trae 报告的核对

原则：Trae 描述「对方有什么」多数属实；把它们写成「**我们项目缺失、建议优先引入**」则多数**不成立**——同文件多已在本仓，且部分已更进一步。

| # | Trae 主张 | 核对 | 说明 |
|---|-----------|------|------|
| 1 | AI 助手 6 组件架构（ChatArea 总线 / DataDrawer / 富消息 / Input…）是高价值缺失项 | **不建议跟「缺失」叙事** | 六文件与本仓 **SHA256 完全一致**；`AssistantPage` 仅差：去掉 carbon/ingestion 导航 + header 高度 token。能力已在 `/assistant`，不是「从零引入」 |
| 2 | S02 横向堆叠条形图、行选联动、稳定 ID `S02-RP-*` | **部分对 / 不建议整体跟** | 对方弹窗确有 stack bar + 稳定 ID；本仓 `S02SafetyRiskModal` 仍含同类图表能力，且已走 **地图工作台 + L2**（对方无）。不宜用对方「弹窗主路径」覆盖现路线 |
| 3 | S03 多字段聚合 / 比例条 / 空态 | **部分对 / 暂缓改壳** | 双方都有弹窗；本仓已按内容打磨方案改口径与 demo 闸，**勿回滚对方旧壳** |
| 4 | S04 `splitStatus()` 办理态+时限态 | **对（事实）/ 不建议再引入** | 本仓 `S04MassAppealModal` **已有** `splitStatus` |
| 5 | G01 三态分布 | **部分对 / 暂缓** | 弹窗已在；本仓短标与文案已对齐甲方，勿换对方分布叙事冲击首页入口 |
| 6 | G02 到期分桶（已逾期 / 7日 / 8-15 / 16-30 / 30日…） | **对（事实）/ 不建议再抄** | 本仓 `G02LicenseModal` **已有同构分桶**；内容打磨已统一「30日内到期」文案 |
| 7 | G03 双标签、G04 纯表+动态筛 | **部分对 / 暂缓** | 已有弹窗；本仓已打磨，勿整包替换 |
| 8 | `useMotionMode`（motion=off / reduced-motion） | **对（事实）/ 已具备** | 与本仓文件 **完全一致**；范围注释写明偏 `/ui-master/dashboard` |
| 9 | `MasterKpiSubCard` rAF 数字滚动 600ms | **对（事实）/ 已具备** | 与本仓 **完全一致**；落在母版 KPI，不是首页 `TopKpiGroups` 缺口 |
| 10 | GIS：CoordinateAdapter / dataMode / PickManager / loadVersion / SpatialAssetStore | **部分对 / 增强须谨慎** | `CoordinateAdapter` **一致**；`TrafficGisOverview` 本仓更大（已接 E/S 工作台）。`SpatialAssetStore` 对方略厚，可作后续对照，**禁止整模块回退覆盖** |
| 11 | ESG 修改记录 Schema「9 张新表」 | **部分对（缺目录）/ 可引入** | 对方 `mysql_build_v1.0_esg_modification` **本仓无此目录**；实为 **10** 张表（Trae 说 9 略不准）：含业务扩展 4 + 导入审计 4 + 碳边界/核算 2 |
| 12 | 「AI 不直接写主表」智能入库文档 | **对（原则）/ 本仓文档已有** | `server/intelligent_ingestion/README.md` 已写分层入库；对方 schema 是该原则的**表结构补强**，不是全新理念 |
| 13 | 统一弹窗分发 / 缩放 / 验收模式 / 语义色 | **对 / 已在用** | 同意「模式可复用」；无需从 RAR 再拷贝 |

**总评：** Trae 对对方代码细节的观察大多准确，但优先级建立在错误前提「首页/弹窗/助手我们都缺」上——**夸大了引入紧迫性**；真正相对本仓的缺口集中在 **入库扩展 Schema、独立碳足迹页壳、ingestion 路由页**，以及个别 GIS/上传组件体积差，而非助手与 S/G 弹窗重做。

---

## 3. 值得引入（按优先级）

> 约束：不改首页 KPI 数值链路、不替换已交付弹窗观感、不打断 E01–E04 / S02 工作台路线。只谈横切能力或独立页/基础设施。

### P0 — ESG 修改报告 Schema（审计/入库分层）

| 项 | 内容 |
|----|------|
| **价值** | `template_definition` / `import_batch` / `import_row_staging(raw_json+normalized_json)` / `validation_result` + 碳边界版本，支撑「模板识别 → 暂存 → 核验 → 再入主表」；与现有 intelligent_ingestion 文档一致，补齐可执行 DDL |
| **为何不影响首页** | 纯 `server/` 新增目录与迁移；默认不接 Dashboard KPI、不改 modal |
| **落点** | 新轨：`server/mysql_build_v1.0_esg_modification/`（或迁入 `server/migrations/` 独立版本前缀）；Issue 单独说明「只加表、不删数据」 |
| **注意** | 表数以 SQL 为准（10）；业务扩展表（产值/设计变更/专项方案/廉政）是否启用要甲方拍板，可先只落地导入四表+边界两表 |

### P1 — 独立「数据智能上传」页路由恢复（壳层）

| 项 | 内容 |
|----|------|
| **价值** | 对方 `DataIngestionPage` 仅是 Header + `WorkspaceSmartUpload` 的专页；本仓上传能力已在 `/workspace`，专页利于演示/验收与助手分流 |
| **为何不影响首页** | 仅加 `/ingestion` 路由与导航项（或仅深链），**默认不改首页 KPI 卡**；上传组件继续写 staging，不直写首页快照 |
| **落点** | `src/views/DataIngestionPage.vue`（可从 RAR 复刻壳）+ `router`；导航是否露出单独确认 |
| **注意** | 对方 `WorkspaceSmartUpload.vue` 体积更大——若要对 diff，在 **workspace/ingestion 轨**做，禁止顺带改 Dashboard |

### P2 — 助手页产品化增强（不拷贝对方组件树）

| 项 | 内容 |
|----|------|
| **价值** | 六组件已齐；后续价值在真实问答、数据依据与 KPI/工作台深链，而非再贴一份 UI |
| **为何不影响首页** | 只动 `/assistant` 与 mock/API 契约 |
| **落点** | `AssistantPage` / `assistant.mock` / 后端问答接口；DataDrawer 对接现有指标口径文档 |
| **注意** | Trae「优先整包引入 assistant/」→ **改为「已具备，开增强 Issue」** |

### P3 — GIS 基础设施对照增强（可选）

| 项 | 内容 |
|----|------|
| **价值** | 对方 `SpatialAssetStore` 等可对照 remote/localStorage 降级完整性；`loadVersion` 本仓已有 |
| **为何不影响首页** | 仅改 `traffic-gis-overview` 资产层，且须跑 E01/S02 点选回归 |
| **落点** | `src/modules/traffic-gis-overview/assets/*`；小步 PR |
| **注意** | **禁止**用对方整份 `TrafficGisOverview.vue` 覆盖本仓（本仓已接工作台，体量大很多） |

### P4 — `/carbon` 独立页（暂缓到有单独需求）

| 项 | 内容 |
|----|------|
| **价值** | 对方有碳足迹专题页壳；与首页 E04/碳效益弹窗是另一呈现 |
| **为何可能影响** | 易与已交付 E04 口径、首页碳相关入口打架；页内硬编码演示数风险高 |
| **落点** | 若做：独立路由 + 明确「不替代 E04 工作台/弹窗」 |
| **建议** | **暂缓**，除非产品要顶栏「碳足迹管理」入口 |

---

## 4. 不建议引入或暂缓

| 项 | 原因 |
|----|------|
| 整包「引入」assistant 六组件 / useMotionMode / MasterKpiSubCard | 已与对方一致；重复引入零收益，易制造无意义 diff |
| 用对方 S02 弹窗条形图主路径替换本仓 S02 地图工作台 | 与 E01 式工作台路线冲突；至多「图表要素择优迁入 L1 面板」，且单开 Issue |
| 回滚或整文件替换 S03/S04/G01–G04 弹窗 | 本仓 2026-07-25 内容打磨已落地；对方稿更旧 |
| 用对方 `DashboardPage.vue` 覆盖本仓 | 本仓已集成 E/S 工作台，对方页小很多；覆盖会毁掉已交付能力 |
| 把数字滚动/动画默认铺到首页 `TopKpiGroups` | 对方能力在母版路由；强行上首页会扰动验收截图与 `acceptance`/`motion` 约定 |
| 把对方 mock KPI / 碳页硬编码指标并入首页数据 | 直接违反「首页数据冻结」 |
| 提交 RAR 内 `node_modules`/`dist`/完整 vendor 树 | 体积与许可证噪音；只允许按需摘抄少量自有源文件并走 PR |

---

## 5. 明确原则（冻结边界）

1. **首页 KPI 数据与已交付弹窗/工作台冻结**：E01–E04、S01（数据层+回退 UI）、S02 工作台 V0.2、S03–G04 内容打磨——默认只读，不为「对齐对方」而改。  
2. **只引入横切能力或助手/基础设施**：入库 Schema、ingestion/assistant 专页增强、GIS 资产层小步对照。  
3. **对方 = 同仓旧快照，不是外部标杆整仓合并**：逐项摘取，禁止整目录覆盖。  
4. **任何引入必须独立 Issue/分支**，验证不含「首页 KPI 数值回归」；禁止把解压代码直接 commit 进 `main`。

---

## 6. 建议的下一步（供拍板）

1. **现在可开：** P0 Schema 只加表（先导入审计 4 表 + 碳边界 2 表，业务扩展 4 表可选）。  
2. **产品确认后开：** P1 `/ingestion` 专页是否要顶栏入口（还是继续藏在 `/workspace`）。  
3. **单独产品轮次：** P2 助手真实能力；P3 GIS 资产层对照。  
4. **默认不做：** 弹窗/首页 KPI/工作台「按对方重做」。

---

## 附：本仓相对对方的已超前能力（避免误「补课」）

- E01 / E02 / E03 地图工作台 + L2  
- S02 地图工作台 + L2 + V0.2 seed  
- `server/e_group` 与 V1.1 迁移族  
- S03–G04 内容打磨与 demo 闸  
- 更大的 `DashboardPage` / `TrafficGisOverview` 集成面
