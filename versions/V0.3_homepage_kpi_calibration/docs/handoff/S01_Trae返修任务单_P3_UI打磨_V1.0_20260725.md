# S01 Trae 返修任务单 · P3 UI 打磨（门禁 105 · 签收后抛光）

**日期：** 2026-07-25  
**版本：** P3-UI-Polish / V1.0（**开工令**）  
**状态：** **已发单 · READY_for_trae**（P1/P2/P3 已 `temp_complete`；本单仅 UI/文案抛光）  
**门禁：** 105（设计已冻结；本单不改口径，只修展示偏差）  
**权威设计：** `_handoff/S01_连续安全生产天数设计说明_B方案_V1.0冻结稿_20260724.md`（**§8** 信息结构与删除项）  
**原 P3 开工令：** `_handoff/S01_Trae实施任务单_P3_V1.0_20260724.md`  
**签收基线：** `交付_S01_P2P3_API与UI实施包/`；状态机 `_handoff/handoff_status.json`（`s01_phase=temp_complete`）  
**实施方：** Trae  
**本单范围：** **仅** `S01SafetyProductionModal` UI/文案打磨；阶段副文案若错，允许最小改动 seed/`stage_detail` 或 API 字段透出。**不开** P1/P2 Schema/API 合同重开，除非为修正阶段副文案所必需。

> 不得把冻结稿直接当 Issue。口径变更走设计变更。本单是签收后抛光，不是重做 P3。

---

## 0. 前置条件

| 项 | 要求 |
|----|------|
| P1 | **PASS**（`server/migrations/s_group_s01_v1_0/`；SPR 77 / DEMO-S01-20260724 / 路基桥涵施工 current） |
| P2 | **PASS**（`_resolve_s01_snapshot`；`GET /api/dashboard/kpi/S01` 同源） |
| P3 | **PASS / temp_complete**（B′ 模态已上线；本单在其上打磨） |
| 冻结 §8 | 只读遵循：§8.1 壳、§8.2 四段结构、§8.3 **禁止恢复**删除项、§8.4 首页卡不改结构 |

动手前先打开并核对：

1. `src/components/modal/S01SafetyProductionModal.vue`
2. 冻结稿 §3.4（最近一次重置）、§8.2 / §8.3
3. `server/migrations/s_group_s01_v1_0/V1_0_030__s01_demo_seed.sql`（CSR `stage_detail` / SPR `current_stage_detail`）
4. `server/mysql_api.py` 中 `_resolve_s01_snapshot` → `currentStageDetail` 取值路径

---

## 1. 范围与非范围

### 1.1 范围内（本单必做）

- 修正阶段副文案（若 API/库内错误）→ **「路基填筑、桥梁基础及下部结构施工」**
- 右侧辅区标题统一为 **「最近一次重置」**（不得出现「最近一次事故」）
- 消除主区时间轴「中间大空」：加宽时间轴内容区 **或** 收紧布局 / 加宽侧栏约 **380–400px**
- 「本轮结论」迁出侧栏 → 页脚上方底部横幅；侧栏仅保留 **3 块**：工期阶段 / 最近一次重置 / 确认状态
- 去掉模态内纵向滚动条，时间轴一屏内完整可见（对齐冻结 §8.1）
- 连续天数主值用 S 组蓝 **`#2f9cff`**；状态徽章继续用绿/黄/红语义色
- 确认状态块保留文案前缀 **「批次：」**

### 1.2 非范围（禁止扩）

- 不重开 P1 Schema / 迁移门禁验收口径（77 / 2026-05-08 / 2026-07-24）
- 不重开 P2 API 字段合同（除为透出正确 `currentStageDetail` 的最小修复）
- 不改首页 S01 KPI 卡片 DOM/版式（冻结 §8.4）
- 不做 E01–E03 GIS 工作台、引导线、「发起督办」
- 不恢复冻结 §8.3 已删交互/组件（见 §7 禁止项）
- 不更新 P4 债务项（旧 smoke 断言 368、`KpiDetailModal` 死代码清理）除非顺手且零风险——默认不做

---

## 2. 必改项（按优先级）

### P0 — 文案正确性（阻断签收）

#### P0.1 阶段副文案纠偏（禁止前端硬编码）

**现象（审查）：** 辅区「当前工期阶段」下副文案出现错误/乱码类文案（审查记为「高速路机、桥墩路校…」）。  
**目标文案：** `路基填筑、桥梁基础及下部结构施工`

**执行要求：**

1. 先查 `GET /api/dashboard/kpi/S01` 的 `currentStageDetail`（及 `currentStage` / `currentConstructionStage`）。
2. 若 API 已错：修 **DB seed / 现网 CSR `stage_detail`（及必要的 `detail`）**，或修 `mysql_api.py` 读取字段，使响应正确。  
   - 种子参考：`V1_0_030__s01_demo_seed.sql` 中 CSR 已写目标文案；若现网仍错，补幂等 UPDATE 或对齐 SPR `current_stage_detail` 与 CSR。  
   - 注意：`ensure_s01_business_tables()` / 旧测数仍可能种「路基｜桥梁｜隧道并行施工」——**不得**让其覆盖当前 demo 有效阶段副文案。
3. **禁止**在 `S01SafetyProductionModal.vue` 内写死该字符串覆盖 API。
4. 阶段主名保持 **「路基桥涵施工」**（来自 API）。

#### P0.2 右侧标题：「最近一次重置」

- 辅区第二块标题必须为 **「最近一次重置」**，不得为「最近一次事故」。
- 无重置时正文/灰注对齐冻结 §3.4（如：「自开工令起未发生触发重置的人员死亡安全生产责任事故」）。
- 若仓库源码已是正确标题，交付报告写明「已核验无回归」并附截图/DOM 证据即可。

---

### P1 — 布局结构（主体验）

#### P1.1 消除主区中间空洞

**现状线索：** `.s01-content` 为 `minmax(0,1fr) 330px`；时间轴区易显空、侧栏偏窄。

**允许手段（择一或组合，勿堆砌组件）：**

- 加宽时间轴可用宽度（节点/标签排版更舒展）；和/或
- 侧栏加宽至约 **380–400px**；和/或
- 收紧上下 padding / 节点 `min-height`，让时间轴视觉填满主区

**禁止：** 为填空而恢复 §8.3 删除件（宫格、规则说教、证据表、伪趋势图、督办按钮等）。

#### P1.2 「本轮结论」迁至底部横幅

| 位置 | 内容 |
|------|------|
| **侧栏（仅 3 块）** | ① 当前工期阶段 ② 最近一次重置 ③ 确认状态（含「批次：…」） |
| **底部横幅** | 「本轮结论」整块移出侧栏，置于 **footer 上方**（一句业务结论 + 既有结论文案绑定） |
| **footer** | 资料来源 / 更新时间 / 核验状态 / 关闭 — 保持 |

结论语义仍对齐 API `conclusion`（演示无重置口径不变）。

---

### P2 — 视觉与滚动（抛光）

#### P2.1 去掉模态内纵向滚动

- 冻结 §8.1：**不设置模态内部纵向滚动条**；时间轴须在 **1436×880** 一屏内完整展示。
- 现状线索：`.timeline-container { overflow-y: auto; … }` —— 改为不出现内滚（`overflow: hidden` 或等价），通过布局压缩/节点间距保证一屏放下。
- 外层 overlay 亦不因内容撑出双滚动。

#### P2.2 天数主色 = S 组蓝

- 摘要「连续安全生产天数」**数字**颜色：`#2f9cff`（与 `THEME_COLOR` 一致）。
- **保留绿色**用于状态类徽章/「已确认」「未发生」等语义状态（`STATUS_COLORS.normal` 等），勿把状态也改成蓝。

#### P2.3 批次文案

- 确认状态块继续使用 **「批次：」** 前缀 + `DEMO-S01-20260724`（演示）。

---

## 3. 必改 / 可能改文件

**主改（几乎必动）**

| 文件 | 动作 |
|------|------|
| `src/components/modal/S01SafetyProductionModal.vue` | 布局（侧栏宽、结论横幅、去内滚）、天数色、标题核验 |

**按 P0.1 诊断结果最小改动（仅当副文案 API 错）**

| 文件 | 动作 |
|------|------|
| `server/migrations/s_group_s01_v1_0/V1_0_030__s01_demo_seed.sql` | 幂等保证 CSR `stage_detail`/`detail` = 目标文案；必要时同步 SPR `current_stage_detail` |
| 或新增同目录补丁 SQL（若不宜改已跑 030） | 幂等 UPDATE；须可重跑 |
| `server/mysql_api.py` | 仅当读错字段/未读 CSR `stage_detail` 时修正透出；**不改**主值/确认/闸门合同 |

**一般不要改**

- 首页 `KpiCard.vue` 结构、其他 KPI 模态
- `e_group` 迁移
- P2 正式/`S01_ALLOW_DEMO` 闸逻辑（除非副文案路径被错误短路）

---

## 4. 必须保留（不得回归）

验收时逐项核对，任一回退即本单 FAIL：

| # | 保留项 |
|---|--------|
| 1 | 标题区 + **「演示数据」** 角标（演示闸开时） |
| 2 | 顶部摘要 **4** 项：连续天数 **77** / 统计起点 **2026-05-08** / 统计期末 **截至 2026-07-24**（或等价「截至」展示）/ 计数状态 **连续计数中** |
| 3 | 主区时间轴节点（开工令、月度确认、待认定/重置如有、统计期末等 B′ 节点） |
| 4 | 阶段主名 **路基桥涵施工** |
| 5 | 批次 **DEMO-S01-20260724**（「批次：」前缀） |
| 6 | footer：来源 / 核验 / 关闭 |
| 7 | 无冻结 §8.3 禁止件回流 |
| 8 | 无前端按日期重算连续天数；正式缺数仍为 `--` |

---

## 5. 验收清单（可勾选）

### P0

- [ ] `currentStageDetail`（API）= `路基填筑、桥梁基础及下部结构施工`；UI 同步展示；**无**前端硬编码覆盖
- [ ] 侧栏标题为「最近一次重置」，无「最近一次事故」

### P1

- [ ] 主区时间轴无大块空洞；侧栏约 380–400px **或** 等价加宽主区方案已落地且观感通过
- [ ] 「本轮结论」在 footer 上方横幅；侧栏仅 3 块（工期阶段 / 最近一次重置 / 确认状态）
- [ ] 未新增 §8.3 禁止组件填空

### P2

- [ ] 模态内无纵向滚动条；时间轴一屏可见
- [ ] 连续天数数字为 `#2f9cff`；状态徽章仍为绿/黄/红语义色
- [ ] 仍显示「批次：DEMO-S01-20260724」（演示）

### 回归（Must keep）

- [ ] 演示角标 + 摘要 77 / 2026-05-08 / 截至 2026-07-24 / 连续计数中
- [ ] 时间轴 + 路基桥涵施工 + footer 来源/核验/关闭
- [ ] `npm run check` / `npm run build` 通过（见下）

---

## 6. 验证命令

```bash
npm ci
npm run check
npm run build
```

若本单改动了 Python（`mysql_api.py` 或迁移 SQL 配套脚本）：

```bash
python -m compileall -q server
```

若改了 seed / 现网阶段副文案，另做：

```bash
# 按环境执行幂等修复后
# 核对 API：
# GET /api/dashboard/kpi/S01 → currentStage / currentStageDetail
```

手工：打开首页 → 点 S01 → 核对本单 §5 全部勾选项；截图放入交付包。

环境限制须在交付说明写明原因与替代验证，**不得**谎报通过。

---

## 7. 禁止项

- 前端硬编码阶段副文案绕过 API/DB
- 恢复冻结 §8.3：6 摘要卡堆叠、30/60/90 节点、12 月宫格、大段规则说教、独立证据表、「发起督办」、「数据可信度」、无数据支撑的四阶段流程/伪趋势图
- 用 ECharts 曲线冒充主时间轴或作为主值来源
- 改首页 S01 KPI 卡结构；改无关 KPI；写 `e_group`
- 前端用「今天」或开工日重算 `continuousDays`
- 正式缺数回退 Mock 368 / 本地 77
- 把本单扩成 P4 全量测试债清理（除非用户另开单）

---

## 8. 交付包约定

目录：`交付_S01_P3_UI打磨包/`

1. **变更文件列表**（相对仓库根路径）
2. **`P3_UI打磨交付报告.md`**，至少包含：
   - P0.1：API `currentStageDetail` 修复前后对照（或「库内已正确、仅 UI 展示核验」证据）
   - P0.2 / P1 / P2 自测说明 + 关键截图（模态全貌、侧栏 3 块、底部结论横幅、无内滚）
   - 是否动到 seed / `mysql_api.py`（动则写清；未动则声明）
   - `npm run check` / `npm run build`（及可选 `compileall`）退出码
   - 已知未做 / 非本单项
3. 分支建议：`trae/105-s01-p3-ui-polish`
4. PR 引用本任务单路径；完成后更新 `_handoff/handoff_status.json`（由签收方或约定流程回写 `s01_p3_ui_polish_status`）

---

## 9. 完成定义（DoD）

- [ ] §5 验收清单全部勾选
- [ ] §4 Must keep 无回归
- [ ] 交付包 `交付_S01_P3_UI打磨包/` 齐全
- [ ] 验证命令通过或书面例外经用户接受
- [ ] 未重开 P1/P2 合同；未恢复 §8.3 禁止项

---

## 10. 给 Trae 的一句话

> P1/P2/P3 已暂时完成；本单只打磨 `S01SafetyProductionModal`：副文案走 DB/API、侧栏三块、结论下沉、去内滚、天数用 `#2f9cff`，并守住 77 / 路基桥涵施工 / DEMO-S01-20260724 / §8.3 红线。
