import{describe,expect,it}from'vitest';import{buildChainage,lineLength}from'../domain/chainage';describe('chainage',()=>{it('measures and interpolates WGS84 route markers',()=>{const line=[[109,24],[109.1,24]];expect(lineLength(line)).toBeGreaterThan(10000);const points=buildChainage([{sectionId:'1标段',coordinates:line}],5000);expect(points.length).toBeGreaterThanOrEqual(2);expect(points[0].name).toBe('K0+000');expect(points[1].properties.sectionId).toBe('1标段')})})
