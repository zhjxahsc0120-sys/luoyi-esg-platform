# E04 Trae 实施任务单 · P1（门禁 104 · V1.0 冻结稿配套）

**日期：** 2026-07-24  
**版本：** P1 / V1.0 · **开工令**  
**门禁：** 104（设计已通过并冻结）  
**权威设计：** `_handoff/E04_累计碳排放工作台设计说明_B方案_V1.0冻结稿_20260724.md`  
**对标参考：** `_handoff/E03_Trae实施任务单_P1_V1.0_20260724.md`（分阶段迁移动作风格；业务域为 E04 累计碳排放，勿抄 E03 水保域）  
**实施方：** Trae  
**本单范围：** **仅 P1**（Schema / 边界与批次 / 因子快照 / 元数据登记 / 自验）。**不含 P2 API**、**不含 P3 UI**。P2、P3 另开任务单，勿混入本单。

> **重要：** 本文件是实现依据，**不得**把设计冻结稿直接当作 GitHub Issue 或替代本单。若需调整边界生效规则、KPI 谓词、精度、快照语义、演示锚定包口径 → **停工 → 设计变更**，不得在实现中自行改口径。

---

## 0. 角色与硬禁令

| 角色 | 职责 |
|------|------|
| Trae | 按本单完成 P1，提交可重复执行的增量迁移 + 自验 SQL/报告 |
| Cursor | 对照冻结稿复核（仅在发现偏离时） |
| Codex | 设计已冻结；P1 完成后参与验收 |

**禁止：**

- **覆盖或重算**已冻结 **12,856.00** 演示勾稽包（月度、来源、成本锚定数不得改动）
- 将 **11,970.96**（候选边界只读对照试算）写入正式 KPI 或替换首页演示主值
- 将 **`BOUND-E04-V1-CANDIDATE`**（`boundary_status=CANDIDATE`）当作当前生效正式边界改演示 KPI
- **`IFNULL(effective_status,'EFFECTIVE')`** 或任何隐式默认把 PENDING/未知计入正式 KPI
- 为演示或正式 **伪造证据文件**、占位证据冒充已通过核验
- 缺少 **`factor_snapshot_id`**（或等价行级不可变快照绑定）的结果行标记 **VERIFIED** 或进入正式 KPI
- 正式 **current** 批次引用非 **ACTIVE** 边界；CANDIDATE **仅只读对照元数据**
- **删除或原位改写**运输历史、演示历史批次结果以「凑」候选边界
- **改写**历史建库脚本（`V1_1_000` 及既有迁移）；仅 **增量**、幂等迁移
- **历史 schema 大改/重写**；活动与边界分离须增量扩展
- 违反 **§6.3**：分量先 `ROUND` 再 `SUM`、前端重算累计、展示值回灌聚合
- 明细级活动项 **双计**、设备类型 **双计**
- 写入 **`e_closure_case`**；不得照搬 E02/E03 闭环地图工作台壳
- 本单内做 **P2（读数 API）/ P3（模态 UI）**

**精度（冻结稿 §6.3，P1 建表/字段须预留）：** 因子 `DECIMAL(20,8)`（或不低于现有精度）；行级中间量 `DECIMAL(24,8)`；批次聚合先 SUM 未舍入中间量再 `ROUND_HALF_UP` 至 2 位。

---

## 1. P1 目标（可独立验收）

在库内形成 E04 核算 **数据模型与登记层**，使后续 P2/P3 能按冻结稿主源/scope/谓词读数：

| 验收项 | 期望 |
|--------|------|
| 演示边界 | 登记 **`DEMO-BOUND-E04-20260718`**，`boundary_status=ACTIVE`（**演示通道**）；与现有 12,856 演示路径一致 |
| 候选边界 | 登记 **`BOUND-E04-V1-CANDIDATE`**，`boundary_status=CANDIDATE`；运输不纳入；**仅元数据 + 只读对照试算引用**，不进正式 KPI |
| 批次模型 | `accounting_batch`（或冻结稿等价表）+ **current 指针**；演示 current 仍产出 **12,856.00** 勾稽 |
| 边界生命周期 | `boundary_status`：`DRAFT` / `CANDIDATE` / `ACTIVE` / `RETIRED`（§5.2.1） |
| 因子快照 | **`factor_snapshot`** 表 + 结果行 **`factor_snapshot_id`** 不可变链接（§5.4） |
| 显式有效性 | `effective_status`、`data_nature`（formal / demo / platform_calc 等）显式落库；正式 KPI 谓词禁止 IFNULL 默认 |
| 证据 | 演示/正式缺文件时 **`evidence_status=MISSING`**；**不得**创建占位文件冒充已有单据 |
| 候选对照 | **仅**候选边界 contrast **元数据**（如约 11,970.96 对照说明字段/配置）；**不得**作为 formal current |

---

## 2. 门禁非阻塞提醒（必须遵守）

历史演示结果若 **不具备完整因子快照链**：

- P1 **允许**在可追溯前提下，用 **库内真实已有** 因子/换算信息 **补链** `factor_snapshot_id`（回填快照链接）
- **禁止**为满足 NOT NULL 或「看起来完整」而 **虚构** 因子来源、版本或证据
- 无法追溯的字段 **诚实标记缺失**（如快照/证据仍 MISSING 或等价待核实态）
- 因补链或元数据登记，**不得**将上述演示记录提升为 **VERIFIED** 或进入正式 KPI
- 可增 **可追溯性元数据**，但 **不得** 改动 **12,856.00** 及已冻结 **月度 / 来源 / 材料 / 成本** 锚定数

---

## 3. 动手前必读

| 位置 | 说明 |
|------|------|
| 冻结稿 §2.2 / §2.7 / §5.2.1 | DEMO vs CANDIDATE；生命周期；对照试算措辞 |
| 冻结稿 §5.4 / §5.5 | 行级因子快照；正式 KPI 谓词（显式 EFFECTIVE） |
| 冻结稿 §6.1 / §6.2 / §6.3 | 固定勾稽表；完整 scope；精度 |
| 冻结稿 §7 | 批次/边界变更与历史保护 |
| 现网 `carbon_*` 表族 | 增量扩展，核对现有演示种子与边界引用 |

---

## 4. 执行步骤（严格按序）

### P1.1 Schema 闸（增量、幂等）

1. 边界配置：扩展或新增边界表/字段，支持 **`boundary_status`** 与 **`DEMO-BOUND-E04-20260718`**、**`BOUND-E04-V1-CANDIDATE`** 登记。
2. 核算批次：批次表 + current 版本指针；演示 current 与 12,856 路径对齐。
3. **`factor_snapshot`** + 结果行 **`factor_snapshot_id`**（FK 或等价约束；不可变语义）。
4. 显式 **`effective_status`**、**`data_nature`**、核验/证据状态字段（按冻结稿 §5.5；**禁止**依赖 IFNULL 默认进正式 KPI）。
5. 候选对照：**仅 metadata**（配置/说明），不写入 formal KPI 聚合。
6. 新增独立迁移文件（命名遵循 `server/migrations/e_group_e01_v1_1/V1_1_0xx__e04_*.sql` 或与现网 `migrate_v1_1.py` 约定一致）；**不得**改写既有历史脚本。

### P1.2 数据登记与补链（不改锚定数）

1. 确保演示边界 ACTIVE、候选边界 CANDIDATE 与冻结稿 ID 一致。
2. 按 §2 **非阻塞提醒** 处理历史演示快照链：仅真实因子补链；不可追溯则保持缺失诚实。
3. **禁止**修改 12,856.00、四来源合计、材料 2,485.73 等 §6.1 锚定。

### P1.3 自验（必须写入交付报告）

- 演示 KPI 路径累计主值仍为 **12,856.00**（同 scope 勾稽 §6.1）
- 候选对照约 **11,970.96** **不在** formal/demo 主 KPI 聚合中作为 current 正式值
- 正式 KPI 谓词 SQL 片段：**无** `IFNULL(...,'EFFECTIVE')` 默认计入
- CANDIDATE 边界未被 current 正式批次引用
- 缺证据记录 **`evidence_status=MISSING`**（无占位文件）

**验证命令（本仓约定）：**

```bash
python -m compileall -q server
```

（P1 以库侧为主；前端 `npm run check/build` 留到 P3。）

---

## 5. P1 完成定义（DoD）

- [ ] 增量 Schema 已上库且幂等；未改写历史建库脚本
- [ ] `DEMO-BOUND-E04-20260718`（ACTIVE 演示通道）与 `BOUND-E04-V1-CANDIDATE`（CANDIDATE）已登记
- [ ] 批次模型 + current 指针就绪；演示路径仍满足 12,856 勾稽
- [ ] `factor_snapshot` + 行级 `factor_snapshot_id` 链接就绪（§5.4）
- [ ] `effective_status` / `data_nature` 显式；正式谓词无 IFNULL EFFECTIVE 默认
- [ ] 证据 MISSING 诚实；无占位文件冒充
- [ ] 候选对照仅为元数据；11,970.96 不进 formal KPI
- [ ] 非阻塞补链遵守 §2：未虚构因子；未因补链将演示标 VERIFIED
- [ ] 自验报告齐备；`python -m compileall -q server` 通过
- [ ] 未实施 P2 API / P3 UI；未写 `e_closure_case`

---

## 6. 下游任务单（尚未在本单授权）

| 阶段 | 任务单 |
|------|--------|
| P2 | `_handoff/E04_Trae实施任务单_P2_V1.0_20260724.md` |
| P3 | `_handoff/E04_Trae实施任务单_P3_V1.0_20260724.md` |

---

## 7. 调度说明

Trae **可在排期允许时启动 E04 P1**；若 **E03 P2/P3** 仍在进行，以项目调度为准，**不得**因 E04 P1 破坏 E03 主线交付。口径冲突 → 停工 → 设计变更。