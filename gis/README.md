# GIS 迁移边界

`gis/current/` 保存当前有效 GIS 模块、地图资源、配置和接口种子资料。

当前前端运行副本仍位于 `frontend/src/modules/traffic-gis-overview/`；`gis/current/` 用于 GIS 模块归属、资料追溯和后续独立迁移评审。两处内容在本次迁移中保持同源复制，不在本阶段重构或替换。

历史 GIS 配置、测试资料和旧交接资料进入 `gis/archive/` 或 `handoff/archive/`，不作为当前运行入口。
