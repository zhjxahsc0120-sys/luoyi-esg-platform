### 6.3 精度规则（冻结意图）

| 层 | 规则 |
|----|------|
| 库内计算 | 统一精度（建议不少于 4 位小数中间量，最终按批次舍入规则一次落地） |
| 接口 | 返回已舍入的权威值；附 `emissionUnit` |
| 展示 | 详情/Tooltip 两位小数；首页摘要可将 12,856.00 显示为 12,856 |
| 禁止 | 各组件分别四舍五入后再相加导致与累计差 0.01；前端重算累计或补尾差 |

---