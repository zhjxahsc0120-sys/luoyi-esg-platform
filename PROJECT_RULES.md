# ESG 项目 AI 开发规则

## 1. 唯一工作区

唯一开发工作区：`C:\ESG_Project`

禁止：

- 在旧工作区开发；
- 创建第二套 ESG 项目；
- 在临时目录或桌面目录修改项目；
- 把旧目录直接全量复制覆盖新工作区。

旧工作区只用于追溯和只读对照。

## 2. 修改前置说明

AI 或开发人员修改前必须先说明：

1. 修改文件；
2. 修改原因；
3. 影响范围；
4. 验证方式。

禁止未经确认：

- 自动重构架构；
- 自动优化首页；
- 修改 API 路径或字段结构；
- 修改未确认业务模块；
- 删除历史资料；
- 将 Demo 数据当作正式业务数据。

## 3. ESG 业务规则

首页一级指标体系及 V0.3 业务事实已冻结。

任何指标的名称、数值、单位、计算口径、描述或风险等级调整，必须先取得人工确认，并记录修改文件和数据来源。

迁移验收完成前，禁止开发或调整：

- S 组详细页面；
- G 组深化页面；
- E 组一级指标口径；
- GIS 替换或新数据源接入；
- 正式数据库迁移。

## 4. 数据规则

- 禁止编造真实业务数据。
- 禁止修改已确认指标口径。
- 禁止删除事实链、业务事实表或已确认字段。
- Demo 数据必须明确标识为 `demo`。
- Demo 与正式数据必须保持来源、环境变量和文档边界清晰。

## 5. 数据库规则

- 当前主链路位于 `database/current/`。
- 历史 build/migration 位于 `database/archive/`，不得作为默认执行入口。
- 任何迁移脚本必须说明业务来源、使用场景和影响范围。
- 禁止直接删除业务事实表、已确认字段或历史迁移资料。
- 数据库结构变更必须单独确认。

## 6. 文件边界

不迁移或不提交：`.env`、密码、Token、上传文件、`node_modules/`、构建产物、缓存、日志、PID、`_tmp*`、`_sec*` 和工具缓存目录。

历史资料进入 `docs/archive/`、`handoff/archive/`、`database/archive/` 或 `gis/archive/`，不得混入当前主链路。

## 7. 验收要求

前端修改至少运行：

```powershell
npm.cmd ci --prefix frontend
npm.cmd run check --prefix frontend
npm.cmd run build --prefix frontend
```

后端修改至少运行：

```powershell
python -m compileall -q backend
```

迁移或目录调整必须验证：

- 迁移清单路径存在；
- SHA256 匹配；
- 前端可启动；
- 后端可启动；
- MySQL 可连接；
- 首页可访问。
